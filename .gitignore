# Windows/macOS/Linux files
Thumbs.db
Desktop.ini
.DS_Store
.directory

# IDEs
.idea/
.vscode/
*.suo
*.ntvs*
*.njsproj
*.sln
*.swp

# Flutter/Dart
**/build/
**/.dart_tool/
**/.flutter-plugins
**/.flutter-plugins-dependencies
**/.packages
**/pubspec.lock
# (Keep pubspec.lock in app if you want reproducible builds, but standard is often to keep or ignore depending on app/library. We will keep pubspec.lock for apps. Wait, let's comment it out or not include it, as keeping it is standard for apps).
**/packages/
**/.pub/

# Android
**/android/local.properties
**/android/key.properties
**/android/app/signing-key.keystore
**/android/app/signing-key.jks
**/*.keystore
**/*.jks

# iOS
**/ios/Flutter/Generated.xcconfig
**/ios/Flutter/flutter_export_environment.sh
**/ios/Pods/
**/ios/Podfile.lock

# Firebase (Sensitive config / Service accounts)
**/google-services.json
**/GoogleService-Info.plist
**/*.pem
**/*.key
**/*credentials*.json
firebase-credentials.json
.firebase/

# Web / Node
**/node_modules/
**/npm-debug.log*
**/yarn-error.log*
.env
.env.local
.env.development.local
.env.test.local
.env.production.local
